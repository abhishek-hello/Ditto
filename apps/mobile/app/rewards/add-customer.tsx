import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function RewardsAddCustomerScreen() {
  return (
    <PlaceholderScreen
      title="Add Rewards Customer"
      figma="63:1988"
      notes="Name + email, duplicate / invalid errors."
    />
  );
}
