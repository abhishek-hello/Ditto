{
  "name": "@ditto/core",
  "version": "0.0.0",
  "private": true,
  "description": "Domain types, zod schemas, money (GBP pence) and UK banking validators shared by every Ditto Pay app.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./money": "./src/money.ts",
    "./uk": "./src/uk.ts",
    "./schemas": "./src/schemas.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf .turbo coverage"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@ditto/typescript-config": "*",
    "vitest": "^5.0.1"
  }
}
